This directory stores Changesets files for nuxt-laravelize releases.

Packages are independently versioned. Each changeset must name every package whose published API or behavior changes; do not add a repository-wide fixed or linked group because existing package versions intentionally differ.
