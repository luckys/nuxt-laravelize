{
  "name": "@nuxt-laravelize/workflows-reliability",
  "version": "0.1.0",
  "description": "Transactional workflow wake-ups through the Laravelize reliability outbox",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "files": ["dist"],
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": { ".": { "types": "./dist/index.d.mts", "import": "./dist/index.mjs" }, "./package.json": "./package.json" },
  "scripts": { "build": "unbuild", "typecheck": "tsc --noEmit", "test": "vitest run", "prepack": "pnpm run build", "pack:test": "pnpm pack --pack-destination ../../.pack" },
  "dependencies": {
    "@nuxt-laravelize/database": "workspace:^",
    "@nuxt-laravelize/reliability": "workspace:^",
    "@nuxt-laravelize/workflows": "workspace:^"
  },
  "devDependencies": { "typescript": "^5.9.3", "unbuild": "^3.6.1", "vitest": "^4.1.5" }
}
