import { index, integer, sqliteTable, text } from 'drizzle-orm/sqlite-core'
import { sql } from 'drizzle-orm'

export const sqliteAuditEntries = sqliteTable('audit_entries', { id: text('id').primaryKey(), schemaVersion: integer('schema_version').notNull(), occurredAt: integer('occurred_at', { mode: 'timestamp_ms' }).notNull(), action: text('action').notNull(), outcome: text('outcome').notNull(), subject: text('subject'), target: text('target'), changes: text('changes'), metadata: text('metadata'), actorType: text('actor_type'), actorId: text('actor_id'), tenantId: text('tenant_id'), locale: text('locale'), executionId: text('execution_id').notNull(), correlationId: text('correlation_id').notNull(), causationId: text('causation_id'), sourceType: text('source_type').notNull(), sourceName: text('source_name'), traceId: text('trace_id'), spanId: text('span_id') }, table => [index('audit_entries_tenant_time_idx').on(table.tenantId, table.occurredAt), index('audit_entries_actor_time_idx').on(table.actorId, table.occurredAt), index('audit_entries_target_time_idx').on(sql`json_extract(${table.target}, '$.type')`, sql`json_extract(${table.target}, '$.id')`, table.occurredAt), index('audit_entries_correlation_idx').on(table.correlationId)])
