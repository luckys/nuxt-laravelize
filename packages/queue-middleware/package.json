{
  "name": "@nuxt-laravelize/queue-middleware",
  "version": "0.1.0",
  "description": "Portable overlap and rate-limit middleware for Nuxt Laravelize queues",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "files": ["dist"],
  "main": "./dist/module.mjs",
  "exports": {
    ".": { "types": "./dist/types.d.mts", "import": "./dist/module.mjs" },
    "./runtime": { "types": "./dist/public-runtime.d.mts", "import": "./dist/public-runtime.mjs" },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "nuxt-module-build build && unbuild --config public.build.config.ts",
    "test": "nuxt-module-build prepare && vitest run",
    "typecheck": "nuxt-module-build prepare && vue-tsc --noEmit",
    "prepack": "pnpm run build",
    "pack:test": "pnpm pack --pack-destination ../../.pack"
  },
  "dependencies": {
    "@nuxt-laravelize/cache": "workspace:^",
    "@nuxt-laravelize/core": "workspace:^",
    "@nuxt-laravelize/queue": "workspace:^",
    "@nuxt-laravelize/rate-limiter": "workspace:^",
    "@nuxt/kit": "^4.4.5"
  },
  "peerDependencies": { "nuxt": ">=4.3.0 <5" },
  "devDependencies": {
    "@nuxt/module-builder": "^1.0.2",
    "nuxt": "^4.4.5",
    "typescript": "^5.9.3",
    "unbuild": "^3.6.1",
    "vitest": "^4.1.5",
    "vue-tsc": "^3.2.8"
  }
}
